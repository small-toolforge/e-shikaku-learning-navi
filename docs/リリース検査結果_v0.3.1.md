# v0.3.1 リリース検査結果

実行環境: Windows 11 / Anaconda base環境

実行方法:

```text
Anaconda Promptから tools/リリース検査.py を直接実行
```

結果:

```text
結果: OK（警告 1件）
```

警告内容:

```text
Node.jsがないためJavaScript構文チェックを省略しました
```

判定:

- 必須ファイル、CSS・JavaScript参照順、主要関数、版番号、Service Worker対象はすべてOK
- Node.jsは本アプリの動作要件ではないため、警告1件はリリース阻害事項としない
- JavaScript構文は実機ブラウザでの起動確認も併用する
